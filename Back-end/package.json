{
  "name": "app-mahasiswa-backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "json-server --watch db.json --port 4000 --routes routes.json",
    "start": "json-server --watch db.json --port $PORT --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
